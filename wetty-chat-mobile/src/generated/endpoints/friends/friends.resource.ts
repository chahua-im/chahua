/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Wetty Chat API
 * Real-time chat application backend API supporting groups, messaging, threads, stickers, invites, and push notifications.
 * OpenAPI spec version: 0.1.0
 */
import type {
  FriendAddInfoResponse,
  FriendRelationshipResponse,
  FriendSettingsResponse,
  ListFriendRequestHistoryParams,
  ListFriendRequestHistoryResponse,
  ListFriendsResponse,
  PendingFriendRequestCountResponse,
} from '../../models';

import { HttpHeaders, httpResource } from '@angular/common/http';
import type { HttpContext, HttpResourceOptions, HttpResourceRef, HttpResourceRequest } from '@angular/common/http';

import { inject } from '@angular/core';
import type { ResourceStatus, Signal } from '@angular/core';

import { map } from 'rxjs';

import { CHAHUA_BASE_URL } from '../chahua.base-url';

export interface OrvalHttpResourceRequestExtension {
  /** Extra headers merged over generated headers. Pass a function to read signals reactively. */
  headers?: HttpResourceRequest['headers'] | (() => HttpResourceRequest['headers']);
  /** Angular HttpContext forwarded to the underlying request. Pass a function to derive it reactively. */
  context?: HttpContext | (() => HttpContext);
  /** Last-resort escape hatch: transform the final request descriptor. Runs inside the resource's reactive context. */
  request?: (request: HttpResourceRequest) => HttpResourceRequest;
}

export type OrvalHttpResourceOptions<
  TValue,
  TRaw = unknown,
  TOmitParse extends boolean = false,
> = (TOmitParse extends true ? Omit<HttpResourceOptions<TValue, TRaw>, 'parse'> : HttpResourceOptions<TValue, TRaw>) &
  OrvalHttpResourceRequestExtension;

function mergeOrvalResourceHeaders(
  base: HttpResourceRequest['headers'],
  extra: NonNullable<HttpResourceRequest['headers']>,
): NonNullable<HttpResourceRequest['headers']> {
  if (!base) return extra;
  if (base instanceof HttpHeaders || extra instanceof HttpHeaders) {
    const toHeaderValue = (value: string | readonly string[]): string | string[] =>
      Array.isArray(value) ? Array.from(value, String) : String(value);
    let merged =
      base instanceof HttpHeaders
        ? base
        : Object.entries(base).reduce(
            (headers, [key, value]) => headers.set(key, toHeaderValue(value)),
            new HttpHeaders(),
          );
    const extraRecord =
      extra instanceof HttpHeaders
        ? extra.keys().reduce<Record<string, string[]>>((record, key) => {
            const values = extra.getAll(key);
            if (values) record[key] = values;
            return record;
          }, {})
        : extra;
    for (const [key, value] of Object.entries(extraRecord)) {
      merged = merged.set(key, toHeaderValue(value));
    }
    return merged;
  }
  return { ...base, ...extra };
}

export function applyOrvalRequestExtension(
  request: string | HttpResourceRequest,
  options?: OrvalHttpResourceRequestExtension,
): HttpResourceRequest {
  const base: HttpResourceRequest = typeof request === 'string' ? { url: request } : request;
  if (!options || (options.headers === undefined && options.context === undefined && options.request === undefined)) {
    return base;
  }
  let next: HttpResourceRequest = { ...base };
  const extraHeaders = typeof options.headers === 'function' ? options.headers() : options.headers;
  if (extraHeaders) {
    next = { ...next, headers: mergeOrvalResourceHeaders(next.headers, extraHeaders) };
  }
  const context = typeof options.context === 'function' ? options.context() : options.context;
  if (context !== undefined) {
    next = { ...next, context };
  }
  return options.request ? options.request(next) : next;
}

type AngularHttpParamValue = string | number | boolean | Array<string | number | boolean>;
type AngularHttpParamValueWithNullable = AngularHttpParamValue | null;

function filterParams(
  params: Record<string, unknown>,
  requiredNullableKeys?: ReadonlySet<string>,
  preserveRequiredNullables?: false,
  passthroughKeys?: undefined,
): Record<string, AngularHttpParamValue>;
function filterParams(
  params: Record<string, unknown>,
  requiredNullableKeys: ReadonlySet<string> | undefined,
  preserveRequiredNullables: true,
  passthroughKeys?: undefined,
): Record<string, AngularHttpParamValueWithNullable>;
function filterParams(
  params: Record<string, unknown>,
  requiredNullableKeys: ReadonlySet<string> | undefined,
  preserveRequiredNullables: boolean | undefined,
  passthroughKeys: ReadonlySet<string>,
): Record<string, unknown>;
function filterParams(
  params: Record<string, unknown>,
  requiredNullableKeys: ReadonlySet<string> = new Set(),
  preserveRequiredNullables = false,
  passthroughKeys: ReadonlySet<string> = new Set(),
): Record<string, unknown> {
  const filteredParams: Record<string, unknown> = {};
  for (const [key, value] of Object.entries(params)) {
    if (passthroughKeys.has(key)) {
      if (value !== undefined) {
        filteredParams[key] = value;
      }
      continue;
    }
    if (Array.isArray(value)) {
      const filtered = value
        .filter(
          (item) =>
            item != null &&
            (typeof item === 'string' ||
              typeof item === 'number' ||
              typeof item === 'boolean' ||
              (item instanceof Date && !Number.isNaN(item.getTime()))),
        )
        .map((item) => (item instanceof Date ? item.toISOString() : item)) as Array<string | number | boolean>;
      if (filtered.length) {
        filteredParams[key] = filtered;
      }
    } else if (value === null && requiredNullableKeys.has(key)) {
      // With a paramsSerializer (preserveRequiredNullables) the literal null
      // is passed through for it to consume; without one, emit an empty
      // string so the required key still reaches the wire as `?key=`
      // instead of being silently dropped. See #3712.
      filteredParams[key] = preserveRequiredNullables ? null : '';
    } else if (value instanceof Date && !Number.isNaN(value.getTime())) {
      // useDates produces Date query params; serialize to ISO so they reach
      // the wire instead of being dropped by the primitive check below. See #3856.
      filteredParams[key] = value.toISOString();
    } else if (
      value != null &&
      (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean')
    ) {
      filteredParams[key] = value;
    }
  }
  return filteredParams;
}
/**
 * @remarks httpResource is available in Angular 19.2 and later.
 */
export function getFriendsResource(
  options: OrvalHttpResourceOptions<ListFriendsResponse, unknown> & { defaultValue: NoInfer<ListFriendsResponse> },
): HttpResourceRef<ListFriendsResponse>;
export function getFriendsResource(
  options?: OrvalHttpResourceOptions<ListFriendsResponse, unknown>,
): HttpResourceRef<ListFriendsResponse | undefined>;
export function getFriendsResource(
  options?: OrvalHttpResourceOptions<ListFriendsResponse, unknown>,
): HttpResourceRef<ListFriendsResponse | undefined> {
  const baseUrl = options?.injector ? options.injector.get(CHAHUA_BASE_URL) : inject(CHAHUA_BASE_URL);
  return httpResource<ListFriendsResponse>(() => applyOrvalRequestExtension(`${baseUrl}/friends`, options), options);
}

/**
 * @remarks httpResource is available in Angular 19.2 and later.
 */
export function getUserFriendAddInfoResource(
  uid: Signal<number>,
  options: OrvalHttpResourceOptions<FriendAddInfoResponse, unknown> & { defaultValue: NoInfer<FriendAddInfoResponse> },
): HttpResourceRef<FriendAddInfoResponse>;
export function getUserFriendAddInfoResource(
  uid: Signal<number>,
  options?: OrvalHttpResourceOptions<FriendAddInfoResponse, unknown>,
): HttpResourceRef<FriendAddInfoResponse | undefined>;
export function getUserFriendAddInfoResource(
  uid: Signal<number>,
  options?: OrvalHttpResourceOptions<FriendAddInfoResponse, unknown>,
): HttpResourceRef<FriendAddInfoResponse | undefined> {
  const baseUrl = options?.injector ? options.injector.get(CHAHUA_BASE_URL) : inject(CHAHUA_BASE_URL);
  return httpResource<FriendAddInfoResponse>(
    () => applyOrvalRequestExtension(`${baseUrl}/friends/add-info/${uid()}`, options),
    options,
  );
}

/**
 * @remarks httpResource is available in Angular 19.2 and later.
 */
export function getMyFriendSettingsResource(
  options: OrvalHttpResourceOptions<FriendSettingsResponse, unknown> & {
    defaultValue: NoInfer<FriendSettingsResponse>;
  },
): HttpResourceRef<FriendSettingsResponse>;
export function getMyFriendSettingsResource(
  options?: OrvalHttpResourceOptions<FriendSettingsResponse, unknown>,
): HttpResourceRef<FriendSettingsResponse | undefined>;
export function getMyFriendSettingsResource(
  options?: OrvalHttpResourceOptions<FriendSettingsResponse, unknown>,
): HttpResourceRef<FriendSettingsResponse | undefined> {
  const baseUrl = options?.injector ? options.injector.get(CHAHUA_BASE_URL) : inject(CHAHUA_BASE_URL);
  return httpResource<FriendSettingsResponse>(
    () => applyOrvalRequestExtension(`${baseUrl}/friends/me/settings`, options),
    options,
  );
}

/**
 * @remarks httpResource is available in Angular 19.2 and later.
 */
export function listFriendRequestHistoryResource(
  params: Signal<ListFriendRequestHistoryParams> | undefined,
  options: OrvalHttpResourceOptions<ListFriendRequestHistoryResponse, unknown> & {
    defaultValue: NoInfer<ListFriendRequestHistoryResponse>;
  },
): HttpResourceRef<ListFriendRequestHistoryResponse>;
export function listFriendRequestHistoryResource(
  params?: Signal<ListFriendRequestHistoryParams>,
  options?: OrvalHttpResourceOptions<ListFriendRequestHistoryResponse, unknown>,
): HttpResourceRef<ListFriendRequestHistoryResponse | undefined>;
export function listFriendRequestHistoryResource(
  params?: Signal<ListFriendRequestHistoryParams>,
  options?: OrvalHttpResourceOptions<ListFriendRequestHistoryResponse, unknown>,
): HttpResourceRef<ListFriendRequestHistoryResponse | undefined> {
  const baseUrl = options?.injector ? options.injector.get(CHAHUA_BASE_URL) : inject(CHAHUA_BASE_URL);
  return httpResource<ListFriendRequestHistoryResponse>(() => {
    const request = {
      url: `${baseUrl}/friends/requests`,
      params: filterParams(params?.() ?? {}, new Set<string>([])),
    };
    return applyOrvalRequestExtension(request, options);
  }, options);
}

/**
 * @remarks httpResource is available in Angular 19.2 and later.
 */
export function countPendingIncomingRequestsResource(
  options: OrvalHttpResourceOptions<PendingFriendRequestCountResponse, unknown> & {
    defaultValue: NoInfer<PendingFriendRequestCountResponse>;
  },
): HttpResourceRef<PendingFriendRequestCountResponse>;
export function countPendingIncomingRequestsResource(
  options?: OrvalHttpResourceOptions<PendingFriendRequestCountResponse, unknown>,
): HttpResourceRef<PendingFriendRequestCountResponse | undefined>;
export function countPendingIncomingRequestsResource(
  options?: OrvalHttpResourceOptions<PendingFriendRequestCountResponse, unknown>,
): HttpResourceRef<PendingFriendRequestCountResponse | undefined> {
  const baseUrl = options?.injector ? options.injector.get(CHAHUA_BASE_URL) : inject(CHAHUA_BASE_URL);
  return httpResource<PendingFriendRequestCountResponse>(
    () => applyOrvalRequestExtension(`${baseUrl}/friends/requests/pending/count`, options),
    options,
  );
}

/**
 * @remarks httpResource is available in Angular 19.2 and later.
 */
export function getFriendRelationshipResource(
  uid: Signal<number>,
  options: OrvalHttpResourceOptions<FriendRelationshipResponse, unknown> & {
    defaultValue: NoInfer<FriendRelationshipResponse>;
  },
): HttpResourceRef<FriendRelationshipResponse>;
export function getFriendRelationshipResource(
  uid: Signal<number>,
  options?: OrvalHttpResourceOptions<FriendRelationshipResponse, unknown>,
): HttpResourceRef<FriendRelationshipResponse | undefined>;
export function getFriendRelationshipResource(
  uid: Signal<number>,
  options?: OrvalHttpResourceOptions<FriendRelationshipResponse, unknown>,
): HttpResourceRef<FriendRelationshipResponse | undefined> {
  const baseUrl = options?.injector ? options.injector.get(CHAHUA_BASE_URL) : inject(CHAHUA_BASE_URL);
  return httpResource<FriendRelationshipResponse>(
    () => applyOrvalRequestExtension(`${baseUrl}/friends/${uid()}`, options),
    options,
  );
}

export type GetFriendsResourceResult = NonNullable<ListFriendsResponse>;
export type GetUserFriendAddInfoResourceResult = NonNullable<FriendAddInfoResponse>;
export type GetMyFriendSettingsResourceResult = NonNullable<FriendSettingsResponse>;
export type ListFriendRequestHistoryResourceResult = NonNullable<ListFriendRequestHistoryResponse>;
export type CountPendingIncomingRequestsResourceResult = NonNullable<PendingFriendRequestCountResponse>;
export type GetFriendRelationshipResourceResult = NonNullable<FriendRelationshipResponse>;

/**
 * Utility type for httpResource results with status tracking.
 * Inspired by @angular-architects/ngrx-toolkit withResource pattern.
 *
 * Uses `globalThis.Error` to avoid collision with API model types named `Error`.
 */
export interface ResourceState<T> {
  readonly value: Signal<T | undefined>;
  readonly status: Signal<ResourceStatus>;
  readonly error: Signal<globalThis.Error | undefined>;
  readonly isLoading: Signal<boolean>;
  /** Guard reads of `value()` with this call: `value()` throws in the error state. */
  readonly hasValue: () => this is ResolvedResourceState<T>;
  readonly reload: () => boolean;
}

export interface ResolvedResourceState<T> extends ResourceState<T> {
  readonly value: Signal<Exclude<T, undefined>>;
}

/**
 * Wraps an HttpResourceRef to expose a consistent ResourceState interface.
 * Useful when integrating with NgRx SignalStore via withResource().
 */
export function toResourceState<T>(ref: HttpResourceRef<T>): ResourceState<T> {
  return {
    value: ref.value,
    status: ref.status,
    error: ref.error,
    isLoading: ref.isLoading,
    hasValue(this: ResourceState<T>): this is ResolvedResourceState<T> {
      return ref.hasValue();
    },
    reload: () => ref.reload(),
  };
}
