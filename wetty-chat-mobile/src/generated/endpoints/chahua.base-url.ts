/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Wetty Chat API
 * Real-time chat application backend API supporting groups, messaging, threads, stickers, invites, and push notifications.
 * OpenAPI spec version: 0.1.0
 */
import { InjectionToken, inject, type Provider } from '@angular/core';

/**
 * Embedded fallback base URL for the `chahua` API, resolved at generation
 * time from the OpenAPI specification's `servers` field (`''` when the
 * specification has no servers).
 */
export const CHAHUA_SERVER_URL: string = '';

/**
 * Strips trailing slashes from a base URL.
 *
 * Generated routes always start with `/`, so normalizing here at the token
 * boundary guarantees `${baseUrl}${route}` can never double or drop the
 * separator between them, for either `HttpClient` services or `httpResource`
 * functions.
 */
export function normalizeBaseUrl(baseUrl: string): string {
  return baseUrl.replace(/\/+$/, '');
}

/** Context passed to a `ChahuaBaseUrlResolver` when it is invoked. */
export interface ChahuaBaseUrlResolverContext {
  /** The explicit `apiId` configured via `override.angular.baseUrl`. */
  readonly apiId: 'chahua';
  /** The embedded fallback server URL (`CHAHUA_SERVER_URL`). */
  readonly serverUrl: string;
}

/** Resolves the runtime base URL for the `chahua` API. */
export type ChahuaBaseUrlResolver = (context: ChahuaBaseUrlResolverContext) => string;

/**
 * Injectable hook for resolving the `chahua` API's base URL at runtime
 * (e.g. from a gateway route registry). Overridden via
 * `provideChahuaBaseUrlResolver`; defaults to the embedded specification
 * server URL.
 */
export const CHAHUA_BASE_URL_RESOLVER = new InjectionToken<ChahuaBaseUrlResolver>('CHAHUA_BASE_URL_RESOLVER', {
  providedIn: 'root',
  factory: (): ChahuaBaseUrlResolver => (context) => context.serverUrl,
});

/**
 * Runtime base URL for the `chahua` API, composed via Angular DI.
 *
 * Precedence: a directly provided value (`provideChahuaBaseUrl`) wins
 * outright; otherwise the `CHAHUA_BASE_URL_RESOLVER` resolver (default or
 * provided via `provideChahuaBaseUrlResolver`) is invoked with the embedded
 * `CHAHUA_SERVER_URL` fallback. The result is always normalized.
 */
export const CHAHUA_BASE_URL = new InjectionToken<string>('CHAHUA_BASE_URL', {
  providedIn: 'root',
  factory: (): string => {
    const resolver = inject(CHAHUA_BASE_URL_RESOLVER);
    return normalizeBaseUrl(resolver({ apiId: 'chahua', serverUrl: CHAHUA_SERVER_URL }));
  },
});

/** Directly provides the `chahua` API's base URL, bypassing the resolver. */
export function provideChahuaBaseUrl(baseUrl: string): Provider {
  return { provide: CHAHUA_BASE_URL, useValue: normalizeBaseUrl(baseUrl) };
}

/** Provides a custom resolver for the `chahua` API's base URL. */
export function provideChahuaBaseUrlResolver(resolver: ChahuaBaseUrlResolver): Provider {
  return { provide: CHAHUA_BASE_URL_RESOLVER, useValue: resolver };
}
